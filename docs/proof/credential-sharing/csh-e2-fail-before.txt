CSH-E2 fail-before proof
Baseline: main 45a1a12 (branch codex/csh-e2-console-templates, tests written first)

1. No template surface on baseline:
$ git ls-files console/src | grep -i template
(no files)

$ grep -rl 'account template' console/src --include='*.ts' --include='*.tsx'
(no match — CSH-V16's token is absent)

2. Verifier on baseline:
FAIL CSH-V16 the console word for creation defaults is template, not preset
Summary: 15 passed, 8 failed

3. The new component tests cannot run on baseline — their components do not exist:
$ pnpm -C console vitest run src/components/accounts/CreateAccountModal.test.tsx src/components/accounts/AccountTemplatesPanel.test.tsx
 FAIL  src/components/accounts/AccountTemplatesPanel.test.tsx
   Error: Failed to resolve import "./AccountTemplatesPanel" from "src/components/accounts/AccountTemplatesPanel.test.tsx". Does the file exist?
 FAIL  src/components/accounts/CreateAccountModal.test.tsx
   Error: Failed to resolve import "./CreateAccountModal" from "src/components/accounts/CreateAccountModal.test.tsx". Does the file exist?
 Test Files  2 failed (2)

--- After implementation ---

4. Component tests pass:
$ pnpm -C console vitest run src/components/accounts
 Test Files  4 passed (4)
      Tests  16 passed (16)
(CreateAccountModal.test.tsx 4: no-template create sends credential_strategy
and no template key; picking a template sends only the template id and removes
the strategy control; clearing the picker restores it; the surface uses the
template word. AccountTemplatesPanel.test.tsx 5: lists, creates, edits,
saves a BYOK rule through the shared policy panel, deletes.)

5. Full console gate:
$ pnpm -C console check
build + typecheck + vitest run: 27 test files, 182 tests, all pass.

6. Verifier after:
PASS CSH-V16 the console word for creation defaults is template, not preset
Summary: 16 passed, 7 failed (remaining conditions owned by F/G/H tasks)

7. Rendered verification (make build, ./starport serve, Chrome at
http://127.0.0.1:8080/accounts):
- The Accounts header offers Templates beside New account.
- The Account templates panel opens, states the stamp-copies rule, lists
  nothing at first, and creates team-default ("Team default") through the
  New template form; the created template lists with its strategy label.
- New account then offers "Start from a template"; picking Team default
  removes the Credential strategy control; creating acme from it lands the
  account with the template's stamped strategy shown in the table and in
  the account detail panel.
