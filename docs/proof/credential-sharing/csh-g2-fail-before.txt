CSH-G2 fail-before proof
Captured: 2026-08-29
Baseline: main fb0c224 + plan commit (branch codex/csh-g2-console-members)

Claim: grants have no operator surface after CSH-G1. No console component
names Members or Teams, and no console view lists users, teams, or a
user's reachable accounts.

$ grep -rn "Members\|Teams" console/src -l
(no output — no members or teams surface exists)

$ bash scripts/verify-credential-sharing.sh | tail -4
PASS CSH-V21 a granted shared credential serves only accounts its grants name, held by tests
FAIL CSH-V22 the console grants an account to a user or a team
FAIL CSH-V23 CI runs this gate and the evidence list names it
Summary: 21 passed, 2 failed

CSH-V22 is the condition this task flips. V23 belongs to CSH-H1.

--- After (same branch, work complete) ---

Server: MembersController mounts under /api/v1/admin — users, teams,
team members, user/team grants, reachable accounts, account-grants
create/delete. Zero identity.Repositories degrade every route to 503
("Identity is not configured"), the template-store pattern. Six
controller tests over a real migrated sqlite store:

$ go test ./internal/server/controllers/ -run 'TestMembers|TestListingUsers|TestTeamLifecycle|TestGrantingAnAccount|TestReachable|TestRemovingAGrant' -count=1
ok  github.com/agentstation/starport/internal/server/controllers 0.397s

Console: /members and /teams routes, MemberDetailPanel (direct grants,
grant form, reachable accounts) and TeamDetailPanel (roster add/remove,
team grants), nav + command palette entries, spaPagePaths grown.

$ pnpm -C console check
Test Files  30 passed (30)
Tests  193 passed (193)
(8 new component tests: 3 member panel, 5 team panel — granting and
the reachable-accounts view both covered)

$ bash scripts/verify-credential-sharing.sh | tail -3
PASS CSH-V22 the console grants an account to a user or a team
FAIL CSH-V23 CI runs this gate and the evidence list names it
Summary: 22 passed, 1 failed

CSH-V22 flipped. V23 remains for CSH-H1, as stated above.

--- Rendered check (identity-enabled gateway, real console build) ---

Started the gateway with a placeholder Google OAuth app so the identity
plane was live, seeded one user through the stored record envelope
(schema_version 1), and drove the shipped console in a browser:

- /members lists the resolved user with subject, name, email, and the
  resolved time; the detail panel grants an account directly and the
  grant appears in both the direct list and the reachable list.
- /teams creates a team inline, opens its panel, adds the member to the
  roster, and grants a second account to the team.
- Reopening the member panel shows the fold: the direct grant and the
  team's grant together under reachable accounts - the same answer the
  gateway resolves for that member's session.

All seeded rows were removed after the check; the store went back to
zero users, teams, memberships, and grants.
