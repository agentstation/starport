CSH-C3 fail-before: the two-credential list test fails on the single-row
drawer (captured 2026-08-29 on branch codex/csh-c3-shared-credential-console
at 5083032, before the SharedCredentialPanel list rendering landed).

 ❯ src/components/credentials/GatewayCredentialPanel.test.tsx (5 tests | 1 failed) 1084ms
   × renders every shared credential with its label and access 1013ms
 Test Files  1 failed (1)
      Tests  1 failed | 4 passed (5)

 ❯ src/components/credentials/GatewayCredentialPanel.test.tsx (5 tests | 1 failed) 1082ms
   × renders every shared credential with its label and access 1013ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  src/components/credentials/GatewayCredentialPanel.test.tsx > renders every shared credential with its label and access
TestingLibraryElementError: Unable to find an element with the text: team-a. This could be because the text is broken up by multiple elements. In this case, you can provide a function for your text matcher to make your matcher more flexible.

Ignored nodes: comments, script, style
[36m<body>[39m
  [36m<div>[39m
    [36m<section[39m

--- After implementation (same branch, pre-commit) ---
pnpm -C console check: 166 tests across 24 files, all green (up 3 from C2's 163).
Campaign verifier: 10/23 PASS including CSH-V10 (ts_all_present 'every account'
'only granted accounts' in SharedCredentialPanel.tsx).
Full gate list: ALL GATES GREEN across the two background batches (first batch
killed externally after 4 passing gates; resumed batch passed the remaining 22
plus repeated none — see task outputs bz0oq6kdf/bxqb0w6s4).
Rendered verify at http://127.0.0.1:8080/providers/anthropic on the rebuilt
binary: drawer Stored group shows the shared-credential empty state ("every
account by default, or only the accounts you grant"); Set credential opens the
modal with the Label field and "Which accounts can use it?" — "Every account"
checked by default, "Only granted accounts" offered.

--- Ship-shape note (2026-08-29) ---
CSH-C3 shipped as two stacked PRs. The autoreview helper refuses a branch
diff that deletes a file whose old side still scores secret-like after
redaction, and the deleted GatewayCredentialPanel.test.tsx tripped that
scan on its mock declarations. PR #287 (merged as 3924f8d) renamed the
panel mechanically — two ~98%-similar rename diffs, no deletion — and
this PR carries the list UI as pure modifications of the renamed files.
The test placeholder "gsk-secret" also became "placeholder-value" so no
known key prefix rides in the bundle.
