# Credential sharing and identity — proof root

Proof records for the CSH campaign (`docs/credential-sharing-plan.html`).

- `csh-a0-fail-before.txt` — the campaign verifier
  `scripts/verify-credential-sharing.sh` at baseline b00c47d:
  `Summary: 0 passed, 23 failed`, exit 1. Every condition names work a
  later CSH task closes; the count becomes terminal at CSH-H1.
