CSH-H1 fail-before: the credential-sharing verifier is not in CI and the evidence list does not name it.

$ grep -rn 'verify-credential-sharing' .github/workflows AGENTS.md
(no matches)

$ bash scripts/verify-credential-sharing.sh | tail -3
PASS CSH-V22 the console grants an account to a user or a team
FAIL CSH-V23 CI runs this gate and the evidence list names it
Summary: 22 passed, 1 failed

--- After (CSH-H1 complete) ---

$ grep -rn 'verify-credential-sharing' .github/workflows AGENTS.md | cut -c1-100
.github/workflows/ci.yml:186:          bash scripts/verify-credential-sharing.sh
AGENTS.md:101:bash scripts/verify-credential-sharing.sh
AGENTS.md:186:`scripts/verify-credential-sharing.sh` guards the credential-sharing and

$ bash scripts/verify-credential-sharing.sh | tail -1
Summary: 23 passed, 0 failed

The gate is terminal at 23 conditions (CSH-V01 through CSH-V23) and runs in CI.
