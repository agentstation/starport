=== CSH-F2 fail-before (baseline main ba523d6 + plan commit f1ec92b) ===
--- CSH-V18 on baseline ---
FAIL CSH-V18 gothic OAuth acquires an identity into the one user model
Summary: 17 passed, 6 failed
--- identity grant is inert on baseline ---
64:	// provider is nil in every shipped build. Nothing in this repository sets
goth is not a dependency on baseline

=== After implementation (2026-08-29) ===

CSH-V18 "gothic OAuth acquires an identity into the one user model": PASS.
verify-credential-sharing.sh: 18 passed, 5 failed (V19-V23 await F3/G/H).

What shipped:
- internal/identity/gothic.go: goth/gothic acquisition. Fixed provider set
  (google, github), one-time claim broker (60s TTL) bridging the callback to
  the localauth identity grant, user upsert by provider-prefixed subject.
- internal/localauth: Gate.UseIdentityProvider fills the registered slot;
  grant docs revised from "inert" to "inert until the composition root
  fills it".
- internal/config: IdentityConfig (STARPORT_IDENTITY_OAUTH_*), secrets
  tagged, callback URL redacted; RuntimeOAuth projection passes
  half-configured providers through so NewGothic refuses at start.
- internal/app.openIdentity: composition step; skipped entirely when
  unconfigured (identity grant stays inert).
- internal/server: ConsoleIdentityController; routes GET
  /console/identity/providers, /{provider}, /{provider}/callback mounted on
  every deployment; nil authenticator answers 503 naming the settings.
- console: identityProviders()/identityBeginPath() in api.ts,
  IdentitySignIn component on the first-contact page (renders nothing when
  the list is empty).

Tests:
- go test ./... : all packages pass.
- internal/identity: 9 gothic tests incl. TestOAuthSignInEndToEnd (full
  Begin -> consent redirect -> callback -> claim -> Authenticate dance with
  a fake goth provider), replay, expiry, tamper, returning-subject.
- internal/localauth: TestAnUnconfiguredGatewayRefusesIdentitySignIn,
  TestUseIdentityProviderFillsTheSlot.
- internal/server/controllers: 6 console_identity tests incl. callback
  mints a verifying session (grant=identity, subject carried) and replayed
  claim refused.
- console: pnpm check green, 28 files / 185 tests.

CSG gate revision (conscious, recorded in the gate header):
- V16 exceptions widened from the two reservation-stating files to the
  identity acquisition surface: internal/identity/, localauth
  grant_identity(_test)/gate, controllers/console_identity(_test), console
  IdentitySignIn. Machine-local surfaces stay in the scan.
- firstContact.test.tsx acceptance revised: first contact now makes exactly
  one credential-free request (the provider list) instead of none.
- CLAUDE.md/AGENTS.md CSG paragraph revised: "registered and inert" ->
  "ships inert and is filled only when an operator configures an identity
  provider".
- verify-console-session-grants.sh: 16 passed, 0 failed after revision.

Acceptance met: TestOAuthSignInEndToEnd signs a configured provider's user
in end to end; an unconfigured deployment is unchanged (openIdentity skips,
grant refuses, console renders no choices) and CSG-revised conditions pass.
